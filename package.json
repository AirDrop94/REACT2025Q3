{
  "devDependencies": {
    "eslint-config-prettier": "^10.1.5",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "lint": "eslint . --ext .js,.ts,.tsx",
    "format": "prettier --write .",
    "prepare": "husky install"
  }
}
